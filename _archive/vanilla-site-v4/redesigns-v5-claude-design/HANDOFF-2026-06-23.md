# HANDOFF — Ajolote Labs website improvement · 2026-06-23

> **▶ RESUME HERE → execute `STAGE-1-RESEARCH-PLAN.md`** (same folder). That file is self-contained: the 17-site list + URLs, the 8-point teardown template, the two-phase capture→writeup method, and output paths. Stage 1 is research/capture-only — no live-site edits. The rest of this handoff is background context.

Purpose: resume cold. Goal is to **improve the live marketing site at https://ajolotelabs.ai/**. This session produced solid research but **drifted off-target into inventing new layouts** instead of improving the real one. This handoff resets to the correct anchor.

---

## ⚡ START HERE (the 3 things that matter)

1. **The live site = the COMMITTED `ajolote-labs-website/index.html`.** Cloudflare Pages deploys on push to `master`. Real layout:
   - Hero **diptych**: big headline left ("Stop losing money to **manual operations.**") + a **"YOUR OPERATIONS · AFTER WE SHIP"** deliverable card right (`one brain · multi-LLM · agents · tools + MCPs`, footer "when we leave: the code is yours…"), kicker "SEC_00 · FORWARD-DEPLOYED ENGINEERS · NEXT.DEPLOY = YOU".
   - → `SEC_01 HOW_WE_WORK` (3 service layers) → `SEC_02 CASE_STUDIES` (2 cards) → footer.
   - **Services/cases are scroll-reveal-gated** — they look empty in a static full-page screenshot but the content IS in the repo and renders on scroll. Do not mistake the page for broken.

2. **There are UNCOMMITTED local edits** in `index.html` + `data/cases.json` (made this session, **not deployed**). **First decision before any work: keep / revert / cherry-pick.** The diff (copy only, layout unchanged):
   - kicker → "ai infrastructure operator · we build it · we run it"
   - card rows → `one brain / ai employees / full audit / you own it`; footer → "we don't ship and walk away…"
   - services H2 → "We build it. We run it. We never leave."; stage copy tightened
   - case metric labels → "Workflows automated" / "AI agents running"; case descriptions reworded
   - footer brand line → "…and we keep operating it with you"

3. **To improve the site: anchor on the real committed layout and improve it IN PLACE. Do NOT invent new layouts.**

---

## What went wrong this session (so we don't repeat it)
- Worked from the local file + assumptions; **never opened the live URL until the end** → drifted.
- **Invented layouts** (a brain/neural visualization, then a console "V1 PRO" instrument panel) instead of improving the actual diptych. *(These exploration files — `experiments/` + `previews/` — were **deleted in the 2026-06-23 cleanup**; this folder is now research-only.)*
- Edited production copy without approval; left it uncommitted.
- Too many multiple-choice questions; under-used then over-used anime.js.
- **Root fix:** ground in the deployed site first → improve the existing layout incrementally → preview + Lighthouse-gate every change → no production edits without approval.

---

## Goal & positioning (canonical — from `vision/`)
- Category: **agent infrastructure operator** — not SaaS, not consultancy. **Build AND operate forever = the business model.**
- Twin site goals (Pablo confirmed): **establish the category** + **convert to discovery calls** (Calendly), equally weighted.
- Vertical-agnostic. **Dark theme only.** No founder/team/about. Anonymized cases (no client logos). All CTAs → `calendly.com/hello-ajolotelabs`. Hold Lighthouse ~100s. **Never estimate time-to-client.**
- Hero lead anchor (Pablo): the **brain/unification** ("one brain over every tool you run"), then operator verb, then the money/pain.

## Research produced — KEEP, it's good (`redesigns/v5-claude-design/`)
- `references/fde-companies/_SYNTHESIS.md` — 13 FDE firms. Distyl = closest **positioning** twin. Patterns: lead with the **operator verb**; **anonymized-metrics proof**; productized first stage; "works with your stack."
- `references/fde-companies/openai-anthropic-fde.md` — best **copy steals**: *"throughout the lifecycle of an engagement"*, MCP-native deliverables ("MCP servers, sub-agents, agent skills in production"), the wedge: labs say "we deploy" → Ajolote says "we deploy **and operate it forever**."
- `references/ux-ui/_SYNTHESIS.md` (see **§e PHASE-A VERIFIED** + `captures/` screenshots) — the 5 brand anchors, **directly verified 2026-06-23:**
  - **Ramp & Distyl are LIGHT** (not dark) → Ajolote's dark + *abstract telemetry* is a genuine differentiator.
  - **Palantir/Anduril** are dark but lean on **documentary video** Ajolote can't shoot.
  - **Distyl proves the no-logos answer:** "Trusted by leaders across telecom, healthcare, manufacturing…" + one big metric, zero named logos.
- `brief/00-goals.md`, `01-information-architecture.md`, `02-review-and-direction.md` — goals, IA, adopt/adapt/reject matrix, and the **"Instrument Panel" direction** (treat as a *proposal*, NOT a decision).
- **READ FIRST:** `design-system/DESIGN.md` + `brand-references/REFERENCES.md` — the brand's own authoritative system. It explicitly **rejects brain icons / "make it pop" / light mode**, and names the 5 anchors.

## Re: "why only 3–5 research sites?"
`REFERENCES.md` deliberately caps at **3–5 anchors** (Palantir/Anduril/Sierra/Ramp/Linear) — it states "15 references average into mud." Separately, the FDE study covered **13** companies. I visually captured 3 of the 5 (the JS-rendered ones needing verification) + Distyl; Sierra/Linear were already cleanly fetched. **This is not a hard ceiling** — broader or rotated UX/UI research is an easy expansion (REFERENCES.md even has a rotation playbook). Decide research breadth in planning.

## Reusable components ALREADY BUILT (improve FROM these, don't rebuild)
`.panel`, `.status-pill`, `.mcell` (+ `[data-counter]`/`[data-fill]` count-up + bar-fill JS), `.stage`, `.case`, cascade word-reveal, marquee gradient, scroll-snap, IntersectionObserver reveals, carousels, sticky CTA, magic-card cursor. Every `main.js` behavior is framework-agnostic. Tokens live in `css/style.css :root`.

## File map
- **LIVE SITE (the target):** `ajolote-labs-website/{index.html, css/style.css (+style.min.css, bump ?v=), js/main.js, data/cases.json}`
- Design system: `ajolote-labs-website/design-system/DESIGN.md`
- Research/brief/explorations: `ajolote-labs-website/redesigns/v5-claude-design/`
- Push guard: `ajolote-labs-website/.claude/hooks/website-preflight.py` (forbidden phrases, stale min.css, overflow)
- Build/serve: `npm run dev` (http.server), `npm run build:css` (esbuild → bump cache-bust), `npm run audit` (Playwright)

## Open decisions for planning
1. **Uncommitted edits** — keep / revert / cherry-pick (do this first).
2. **Scope** — improve live `index.html` in place vs. build fresh then promote. (Pablo leaning: improve the real layout.)
3. **anime.js** (repo: github.com/juliangarnier/anime, v4.5, MIT, ESM) — vendor locally + Lighthouse-gate IF adopted; NOT required for reveal/count-up motion (CSS + IntersectionObserver already cover it). Adopt only if a signature needs true scroll-scrubbing.
4. **Real anonymized case numbers** — current metrics are placeholders; need true/defensible figures before anything ships.
5. **Research breadth** — expand beyond 5 anchors? (Pablo asked.)

## Recommended first moves (next session, in planning mode)
1. Read `design-system/DESIGN.md`; open https://ajolotelabs.ai/ AND the committed `index.html` — confirm the real layout together.
2. Resolve the uncommitted-edits question.
3. Plan **incremental improvements to the REAL layout** — likely highest-value gaps found in research: the missing **engagement/"maintain" story**, a **proof band** (anonymized sector line + count-up metrics, à la Distyl), tighter hero card, motion craft — each previewed + Lighthouse-gated.

## Local servers (will stop when context clears — restart as needed)
- `:8765` site root · `:8766` experiments · `:8767` previews (`python -m http.server <port>` from the respective dir).
