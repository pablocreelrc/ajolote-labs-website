# HANDOFF — ajolotelabs.ai re-platform (v6 product experience) · 2026-06-24

Resume cold. **Goal now: finish the preloader** (an anime.js human-brain "boot" sequence). The bigger build (Next.js product experience) is done at parity and Pablo likes it.

---

## ⚡ START HERE
1. **The new site lives at `redesigns/v6-product/`** — a **Next.js 16 + TS + Tailwind 4** static-export app. It is **NOT deployed**. The **live ajolotelabs.ai is still the old vanilla site** (`../../index.html`), untouched. Cut-over is a later, approved step.
2. **Immediate task = the preloader brain.** A lab with 3 anime.js variants is at `redesigns/v6-product/lab/index.html`. Pablo needs to pick a variant (1 trace / 2 synapse / 3 energy); then **wire the winner into `src/app/Preloader.tsx`** (replacing its current abstract node-core), keeping reduced-motion + the ENTER CTA.
3. **Serve locally** (servers die on context clear — restart): static build `python -m http.server 8797 --directory redesigns/v6-product/out`; brain lab `python -m http.server 8798 --directory redesigns/v6-product/lab`. **Always open previews in real Chrome, not Cursor** (`Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" "<url>"`).

## How we got here (the arc)
- **Stage 1 research (done):** 17 browser-verified competitor teardowns + a baseline teardown of the live site → `redesigns/v5-claude-design/references/` (`operator-peers/`, `anchors/`, `baseline/ajolotelabs.md`, two `_SYNTHESIS.md`). Run via a Workflow.
- **Positioning locked:** **"HappyRobot for Mexico" on the messaging axis** — adopt the AI-workforce content architecture, keep Ajolote's dark instrument-panel identity. See `redesigns/v5-claude-design/STAGE-2-POSITIONING-MAP.md`, `STAGE-2-SECTION-BUILD.md`, `content-v1.md`.
- **5-beat architecture:** Hero → **Thesis** (new) → **Platform** (Brain-center system diagram, new) → **Ownership** (new) → Cases → Closing CTA. (Live site only had Hero→Services→Cases.)
- **Design system updated + research-grounded:** `design-system/DESIGN.md` (positioning, 5-beat rhythm, Platform diagram §4.9, anonymized-proof rule) and `CONSTRAINTS.md` (the blanket "no animation library" was **relaxed to a gated exception** — one vendored, deferred, reduced-motion-gated lib allowed for a *signature* if Lighthouse ≥ 90). Both have "Evidence" pointers to the teardowns.
- **Category pivot:** Pablo decided the site is a **product experience**, not a marketing brochure → re-platformed to Next.js (matches the analyzed peers, all Next.js).
- **Vanilla reference:** `../../preview-v5.html` (at the site root) = the real `index.html` + the platform diagram + breath beats + a 3↔5 section toggle. It's the design reference the React build was ported from.

## v6 build state (Phase 1 = DONE, at parity)
`redesigns/v6-product/` — `src/app/{layout.tsx, page.tsx, globals.css, MotionScript.tsx, Preloader.tsx}`.
- All 5 beats as React; dark tokens from DESIGN.md; **self-hosted fonts** via `next/font/local` (Satoshi/General Sans/JetBrains Mono in `public/fonts/`).
- Motion ported into `MotionScript.tsx` (client): cascade word-rise, gradient-marquee sweep, IntersectionObserver reveals, **count-up** on metrics, staggered **platform-diagram build**.
- **Border-beam (`fx-bb-spin` conic) on every card** — hero deliverable card, both case cards, platform quadrants + brain. Matches live.
- **Hero deliverable card matched to live** copy + size (645×~600, 12px radius, drop shadow, border-beam). Uses the LIVE copy ("after we ship / final state / one brain · multi-LLM · agents · tools + MCPs").
- **Breath beats (Thesis/Ownership) are cyan-only** (accent + background wash) per Pablo — no amber.
- **Static export** (`output:'export'`, `trailingSlash`, images unoptimized) → `out/`; deploys on Cloudflare Pages exactly like today. SSR/no-JS renders all content.

## Preloader state (the active task)
- `src/app/Preloader.tsx` currently = boot sequence: wordmark + cyan progress bar + cycling status (initializing brain → mounting nodes → agents online) + **ENTER SYSTEM CTA** (appears when ready; soft 5s auto-advance) + an **abstract node-core** (placeholder).
- **`lab/index.html`** = the real exploration: **anime.js v4** (vendored at `lab/anime.umd.min.js`; also `npm i animejs` in the project) animating a **defined human brain** (Lucide `brain` SVG, ISC license) across **3 variants** (trace / synapse / energy) with a live switcher.
- Pablo rejected the hand-drawn brain ("not a defined brain") → switched to the Lucide brain, which he's evaluating.
- **Next:** pick variant → port into `Preloader.tsx` (swap the node-core SVG for the Lucide brain + the chosen anime.js timeline; vendored anime.js is fine under the gated exception). Keep reduced-motion fallback + `@media (scripting:none)` no-JS guard + ENTER CTA.

## Open threads / decisions
- **Hero subhead:** v6 uses the *disk/uncommitted* line ("We build one brain… Infrastructure you own…"); the LIVE line is "On the first call we map what you run…". Unresolved — tied to the never-resolved **keep/revert/cherry-pick** of the uncommitted edits in the live `../../index.html` + `data/cases.json`.
- **Lighthouse never run** on v6 — must hit ≥90 (perf/a11y) before cut-over.
- **Case metrics are placeholders** ($500K+/3/30, 46/40/3) — need defensible real numbers before shipping.
- Optional live parity not ported: **cursor-follow glow** (`::after` radial) on cards.
- **Mexico framing** kept geo-marker-light (passes the preflight `forbidden_strings`); explicit-Mexico copy would need loosening that config.

## Key decisions (don't re-litigate)
- Product experience, Next.js static export, Cloudflare Pages. Live site untouched until approved cut-over.
- anime.js allowed for the preloader signature (gated exception, now in CONSTRAINTS.md).
- Literal human brain is OK — no hard "no brain" rule; just avoid the generic-AI-startup cliché (use the clean Lucide line brain, on-brand).
- Cyan everywhere on the beats (no amber). Beams on all cards. Open previews in Chrome.
