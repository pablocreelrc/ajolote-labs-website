# HANDOFF — ajolotelabs.ai v6 · 2026-06-27

Resume cold from here. Supersedes `HANDOFF-2026-06-25.md`.

## ⚡ Where the site stands
- **v6-product is built and verified, NOT deployed.** Live ajolotelabs.ai is still the old vanilla `../../index.html`. No Cloudflare push without Pablo's explicit go (deploy = `git push` master).
- **SEC_02 "one task through the brain" diagram** is ported to production (`page.tsx` + `globals.css`), driven by `PlatformDiagram.tsx` (anime.js v4 dep). Desktop = 5-tile; mobile = vertical sequence (console → Context → Models → AI Employees → Governance, ▼ connectors).
- **Done this week:** removed hero "what we deliver" card (hero now single left-aligned column); removed all `SEC_xx` label strips + nav numbering (now Services / Cases / Contact); fixed footer tagline ("The brain your operation runs on."); fixed mobile nav overlap; applied approved **scorecard Tiers 1–3** (CTA label standardized to "Book a discovery call"; tap targets ≥44px; SEC_02 desktop dead-space tightened; SEC_02 mobile = vertical sequence; preloader 7s→3.5s + visible "tap to skip →").
- **Verified:** `npm run build` clean (static export → `out/`), **Lighthouse 100/100/100/100 desktop + mobile**, no horizontal overflow, 7 Calendly CTAs all → `calendly.com/hello-ajolotelabs` (loads 200), reduced-motion clean.
- **Two adversarial scorecards** rated the whole site ~**60/100** (not ship-ready). Top open themes: **(1) no proof/trust above the fold**, (2) the brain visual reads generic-AI (parked — re-platform bet). Tiers 1–3 addressed the quick/medium items.

## 🎯 THE OPEN TASK (this handoff's whole point) — real, grounded case studies
The case section is the biggest remaining credibility gap. Today `src/app/Cases.tsx` is **hardcoded + qualitative** (no real proof): two cards — "Trade & Distribution → Cross-Border Spirits" (= **Mezcal**) and "Enterprise B2B → Loyalty Platform" (= **Desclub**). `public/data/cases.json` has richer fields + metrics but is **orphaned** (not consumed). We did the competitor research on *how to present by vertical* (`cases-vertical-research.md`) — but the cases are still placeholder copy, not grounded in the actual work.

**Pablo's directive:** before writing the website cases, do **deep research on the real work he performed** in each engagement, write a proper **business case** per client, THEN translate each into the website's case card, presented **by vertical** like the competitors (per `cases-vertical-research.md`). This is how we get *honest* proof (real numbers/outcomes) instead of qualitative placeholders.

### Step 1 — Scan the source folders for the real delivered work
Read-only deep scan (these are siblings of `Ajolote tech/` under `…/Files/`):
- **`../../../Mezcal/`** (Destileria Sacrificio — spirits/CPG, Industrial-SME). Look in: `docs/`, `Sacrificio/`, `Akul/`, `Corporativo/`, `Projects/`, `onboarding-tracker.md`, `saas-inventory-draft.md`, `Sacrificio_Mezcal_Comprehensive_Report (1).pdf`, and the **TWM-Intelligence** app (per `Files/CLAUDE.md` — a 40k+ LOC Python app; locate it, it wasn't at folder top-level). Extract: what was actually built (the company "brain", agents, **SAT integration**, **cash-flow prediction**, inventory), what shipped, what changed, any real numbers/outcomes, and where the "80% automated company" target comes from (it's a TARGET, not achieved — keep that framing).
- **`../../../Desclub/`** (Loyalty Solutions — consumer loyalty/retention turnaround). Look in: `Financieros/`, `Operaciones/`, `Estrategia/`, `Automatización/`, `CRM & Database Marketing/`, `App/`, `docs/`, `DEFINITIONS.md`, `desclub_schema_scan.md`, `Juntas/`. Extract: the turnaround scope, which **manual workflows were automated** (and how many — there may be a real count), the MCP-as-distribution layer, financial-model outcomes, anything defensible as proof.
- **`Ajolote tech/vision/`** (`03-project-tracks.md`, `01-mcp-mexico-beachhead.md`, `02-engagement-playbook.md`) for the engagement framing + library contributions per track.

### Step 2 — Write a real business case per client
For Mezcal and Desclub each: the actual problem, what Ajolote built (brain + agents), the engagement arc, and **real outcomes/metrics where defensible** (mark targets as targets; never invent). Keep these as working docs (e.g., alongside `cases-vertical-research.md`).

### Step 3 — Translate to the website cases (by vertical)
Apply `cases-vertical-research.md`'s **4-beat card** (vertical label → buyer's-words problem → what the agents do → proof) to `Cases.tsx` (+ wire `cases.json` if adding metrics). Use the real numbers from Step 2 as the proof (this finally fills the scorecard "no proof" gap honestly). Then build → verify desktop (`#cases` inline) + mobile (`/cases` route) → Lighthouse holds → show Pablo.

## ⏳ Decisions still open (Pablo)
- **Vertical labels:** research recommends concrete industry labels (Mezcal "Consumer Goods & Cross-Border Trade", Desclub "Consumer Loyalty & Retention"); Pablo leaned toward **keeping current** ("Trade & Distribution" / "Enterprise B2B"). UNRESOLVED — revisit once the business cases exist.
- **Proof model** (from `cases-vertical-research.md §1c`): School A = hard metrics + named client (HappyRobot/Decagon) — **we can't use it as-is** (hard stops: no logos, no fake numbers). School B = testimonial/no-numbers (Sierra) — what we have. The business-case research (Step 2) may surface **real** numbers that let us do an honest School-A-lite. Decide after Step 2.

## 📍 Pointers
- Research: `cases-vertical-research.md` (the by-vertical pattern + proposed copy + proof options), `competitor-research.md` (broader HappyRobot/peers messaging).
- Code: `src/app/Cases.tsx` (hardcoded cards), `src/app/cases/page.tsx` (the `/cases` route, mobile), `public/data/cases.json` (orphaned data + metrics schema), `content-v2.md` SEC_04 (case copy brief).
- Run/verify: `npm run build` → `npx serve out -l tcp://0.0.0.0:8080` → desktop in real Chrome via explicit `chrome.exe`; phone at `http://192.168.1.36:8080` (Windows Firewall + VPN are the usual blockers).
- Architecture/knobs: `ARCHITECTURE.md`. Plan file: `~/.claude/plans/couldnt-we-have-the-hidden-popcorn.md`.
- Hard stops: dark theme only, no founder/team, no client logos, **no invented numbers** (real-or-target only), 6+ CTAs → Calendly, geo-light.
